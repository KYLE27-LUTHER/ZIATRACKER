# Copy this to .env and paste the connection string from your CockroachDB Cloud cluster.
# Keep this value server-side; do NOT put it in index.html.
DATABASE_URL=postgresql://USERNAME:PASSWORD@HOST:26257/defaultdb?sslmode=require&options=--cluster%3DCLUSTER_NAME
PORT=3000
NODE_ENV=production
